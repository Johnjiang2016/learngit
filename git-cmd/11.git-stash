1、git stash  用于隐藏当前工作区域或者缓存区域的修改
2、git stash apply stash@\{0\}  用于恢复指定的修改
   git stash apply 用于恢复最近一次的修改
3、git stash drop 删除最近一次的 隐藏的记录
4、git stash pop 用于恢复最近一次的修改，并且删除记录
5、git stash list 查看所有的隐藏记录
     
    